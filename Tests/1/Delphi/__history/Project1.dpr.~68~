Program Project1;

Uses
    System.SysUtils;

Var
    I, J, Number, InputNum: Integer;
    MyStr: String;

Begin
    MyStr := '';

    ReadLn(MyStr);
    //Insert(MyStr, MyStr, 4);
    //MyStr := UpperCase(MyStr);

    for I := Low(MyStr) to (High(MyStr) - 1) do
        if ((MyStr[i] = 'a') And (MyStr[i + 1] = 'b')) then
        Begin
            MyStr[i] := 'A';
            MyStr[i + 1] := MyStr[i];
        End;



    WriteLn(MyStr + ' affa');

    ReadLn;
End.
