Program Lab2_2;

Uses
    System.SysUtils;

Function GetPrimeNumber(Number: Integer): Integer;
Begin

    Var
        IsNumberOdd: Boolean;
    Var
        Count: Integer;
    Var
        CountOfPN: Integer;
        //Var

    CountOfPN := 2;
    IsNumberOdd := Odd(Number);

    If Number = 1 Then
        GetPrimeNumber := 2
    Else
        If Number = 2 Then
            GetPrimeNumber := 3;

    If Number > 2 Then
    Begin

        While (CountOfPN < Number) Do
        Begin

            Count := Count + 1;
        End;
    End;

End;

Var
    N: Integer;
    IsCorrect: Boolean;

Begin

End.
